<Window x:Class="Hedger.Views.CreateContainerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hedger.Views"
        mc:Ignorable="d"
        Title="CreateContainerView" 
        MinHeight="450" MinWidth="400"
        Height="450" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="42"/>
        </Grid.RowDefinitions>
        
        <StackPanel Orientation="Vertical">
            <Grid Height="{StaticResource StackPanelElementHeight}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="account:" Style="{StaticResource BaseTextBlockStyle}"/>
                <ComboBox Grid.Column="1" ItemsSource="{Binding Accounts}" SelectedItem="{Binding SelectedAccount}"
                          Style="{StaticResource BaseComboBoxStyle}"/>
            </Grid>
            
            <Grid Height="{StaticResource StackPanelElementHeight}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="42"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Basis" Style="{StaticResource BaseTextBlockStyle}"/>
                <TextBox Grid.Column="1" Style="{StaticResource BaseTextBoxStyle}"
                         Text="{Binding BasisName, Mode=OneWayToSource}"
                         ToolTip="полное имя тикера."/>
                <TextBox Grid.Column="2" Style="{StaticResource BaseTextBoxStyle}"
                         Text="{Binding BasisExchange, Mode=OneWayToSource}"
                         ToolTip="биржа тикера."/>
                <Button Grid.Column="3" Width="32" Height="32" Content="+" Style="{StaticResource BaseButtonStyle}"
                        Command="{Binding RequestBasis}"/>
            </Grid>

            <Grid Height="{StaticResource StackPanelElementHeight}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="42"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Hedge Basis:" Style="{StaticResource BaseTextBlockStyle}"/>
                <TextBox Grid.Column="1" Style="{StaticResource BaseTextBoxStyle}"
                         Text="{Binding BasisName, Mode=OneWayToSource}"
                         ToolTip="полное имя тикера."/>
                <TextBox Grid.Column="2" Style="{StaticResource BaseTextBoxStyle}"
                         Text="{Binding BasisExchange, Mode=OneWayToSource}"
                         ToolTip="биржа тикера."/>
                <Button Grid.Column="3" Width="32" Height="32" Content="+" Style="{StaticResource BaseButtonStyle}"
                        Command="{Binding RequestHedgeBasis}"/>
            </Grid>

            <Grid Height="{StaticResource StackPanelElementHeight}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Current basis:" Style="{StaticResource BaseTextBlockStyle}"/>
                <TextBlock Grid.Column="1" Style="{StaticResource BaseTextBlockStyle}">
                    <TextBlock.Text>
                        <MultiBinding StringFormat="{}{0} {1} {2} {3}">
                            <Binding Path="SelectedBasis.Name" />
                            <Binding Path="SelectedBasis.Symbol" />
                            <Binding Path="SelectedBasis.Exchange" />
                            <Binding Path="SelectedBasis.DaysToExpiration"/>
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
            </Grid>
            <Grid Height="{StaticResource StackPanelElementHeight}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Current hedge basis:" Style="{StaticResource BaseTextBlockStyle}"/>
                <TextBlock Grid.Column="0" Text="Current hedge basis:" Style="{StaticResource BaseTextBlockStyle}"/>
                <TextBlock Grid.Column="1" Style="{StaticResource BaseTextBlockStyle}">
                    <TextBlock.Text>
                        <MultiBinding StringFormat="{}{0} {1} {2} {3}">
                            <Binding Path="SelectedBasis.Name" />
                            <Binding Path="SelectedBasis.Symbol" />
                            <Binding Path="SelectedBasis.Exchange" />
                            <Binding Path="SelectedBasis.DaysToExpiration"/>
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
            </Grid>
        </StackPanel>

        <Button Grid.Row="1" Content="create" Style="{StaticResource BaseButtonStyle}"
                Command="{Binding Create}"
                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
    </Grid>
</Window>
