<Window x:Class="Hedger.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hedger"
        mc:Ignorable="d"
        Title="MainWindow"
        Height="450" Width="800"
        MinHeight="450" MinWidth="800">
    
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="connector">
                <MenuItem Header="connect" Command="{Binding Connect}"/>
            </MenuItem>
            <MenuItem Header="containers">
                <MenuItem Header="create" Command="{Binding CreateContainer}"/>
            </MenuItem>
        </Menu>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="42"/>
            </Grid.ColumnDefinitions>
            <ListView DockPanel.Dock="Bottom" x:Name="lvContainers" ItemsSource="{Binding Containers}"/>

            <StackPanel Grid.Column="1">
                <Button Content=">" Width="32" Height="32" Style="{StaticResource BaseButtonStyle}"
                        Command="{Binding StartContainer}"
                        CommandParameter="{Binding ElementName=lvContainers,Path=SelectedItem}"/>
            </StackPanel>
        </Grid>

        
    </DockPanel>
</Window>
