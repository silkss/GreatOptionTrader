<Application x:Class="Hedger.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Hedger"
             xmlns:viewModles="clr-namespace:Hedger.ViewModels">

    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Core;component/Resources/BaseStyleProperties.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <DataTemplate DataType="{x:Type viewModles:ContainerViewModel}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Style="{StaticResource BaseTextBlockStyle}">
                        <TextBlock.Text>
                            <MultiBinding StringFormat="{}{0}: {1}">
                                <Binding Path="Instrument.Name"/>
                                <Binding Path="Container.Account"/>
                            </MultiBinding>
                        </TextBlock.Text>
                           
                    </TextBlock> 
                    
                    <UniformGrid Grid.Column="1" Rows="1">
                        <TextBlock Text="{Binding BidPrice}" Style="{StaticResource BaseTextBlockStyle}"
                                   TextAlignment="Right"/>
                        <TextBlock Text="{Binding AskPrice}" Style="{StaticResource BaseTextBlockStyle}"
                                   TextAlignment="Right"/>
                        <TextBlock Text="{Binding LastPrice}" Style="{StaticResource BaseTextBlockStyle}"
                                   TextAlignment="Right"/>
                    </UniformGrid>
                </Grid>
            </DataTemplate>
        </ResourceDictionary>
    </Application.Resources>
</Application>
