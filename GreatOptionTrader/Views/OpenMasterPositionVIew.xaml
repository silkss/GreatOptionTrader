<Window x:Class="GreatOptionTrader.Views.OpenMasterPositionVIew"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GreatOptionTrader.Views"
        mc:Ignorable="d"
        Title="OpenMasterPositionVIew" 
        Height="450" Width="400"
        MinHeight="450" MinWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="42"/>
        </Grid.RowDefinitions>
        <StackPanel >
            <UniformGrid Grid.Row="0" Rows="1" x:Name="ugOrderParams">
                <TextBox x:Name="tbVoolume"
                         Text="{Binding Volume,Mode=TwoWay}"
                        Height="{StaticResource StackPanelElementHeight}"
                        Style="{StaticResource BaseTextBoxStyle}"/>
                <TextBox x:Name="tbPrice"
                         Text="{Binding Price,Mode=TwoWay}"
                            Height="{StaticResource StackPanelElementHeight}"
                            Style="{StaticResource BaseTextBoxStyle}"/>
                <ComboBox x:Name="cbTradeDireation" 
                          SelectedItem="{Binding Direction,Mode=TwoWay}"
                            ItemsSource="{Binding Source={StaticResource TradeDirectionToItemSource}}"
                            Height="{StaticResource StackPanelElementHeight}"
                            Style="{StaticResource BaseComboBoxStyle}"/>
            
            
            </UniformGrid>
            <Button Grid.Row="1" Content="send order" Style="{StaticResource BaseButtonStyle}"
                    Click="sendMasterOrder"/>

            <Grid Height="{StaticResource StackPanelElementHeight}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="target PnL:" 
                        Style="{StaticResource BaseTextBlockStyle}"/>
                <TextBox x:Name="tbTargetPnL" Grid.Column="1"
                        Style="{StaticResource BaseTextBoxStyle}"/>
            </Grid>
        </StackPanel>

    </Grid>
</Window>
