<Window x:Class="GreatOptionTrader.Views.EditInstrumentGroupView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GreatOptionTrader.Views"
        mc:Ignorable="d"
        Title="Модифицирование группы." 
        Height="450" Width="500"
        MinHeight="450" MinWidth="500">
    <Grid>
        <StackPanel x:Name="spContainer">
            <Grid Height="{StaticResource StackPanelElementHeight}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="account:" Style="{StaticResource BaseTextBlockStyle}"/>
                <TextBlock Grid.Column="1"
                           Text="{Binding Container.Account}"
                           TextAlignment="Center"
                           Style="{StaticResource BaseTextBlockStyle}"/>
            </Grid>
            <Grid Height="{StaticResource StackPanelElementHeight}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="position:" Style="{StaticResource BaseTextBlockStyle}"/>
                <TextBlock Grid.Column="1" 
                           TextAlignment="Center"
                           Text="{Binding Position.CurrentVolume}"
                           Style="{StaticResource BaseTextBlockStyle}"/>
            </Grid>
            <Grid Height="{StaticResource StackPanelElementHeight}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1.5*"/>
                    <ColumnDefinition Width="1.5*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="market data:" Style="{StaticResource BaseTextBlockStyle}"/>
                <TextBlock Grid.Column="1" Text="{Binding BidPrice}"
                           Style="{StaticResource BaseTextBlockStyle}" TextAlignment="Center"/>
                <TextBlock Grid.Column="2" Text="{Binding AskPrice}" 
                           Style="{StaticResource BaseTextBlockStyle}" TextAlignment="Center"/>
            </Grid>
        </StackPanel>
    </Grid>
</Window>
